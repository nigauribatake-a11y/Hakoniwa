{
  "name": "hakoniwa-modern",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -b",
    "db:turn": "pnpm run build && node packages/db/dist/advance-turn-db.js",
    "db:seed:sql": "pnpm run build && node packages/db/dist/seed-sql.js",
    "test": "pnpm run build && node --test packages/core/dist/**/*.test.js packages/db/dist/**/*.test.js"
  },
  "dependencies": {
    "pg": "^8.21.0"
  }
}
